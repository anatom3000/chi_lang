import .math
import .utils

def main() {
    let a = math.add(3, math.add(2, 4));

    let origin = math.point.Point {
        x: 0.0,
        y: 0.0,
    }

    utils.printf("math: 2+3+4=%i", a)
    utils.printf("The origin is at: (%i, %i)", origin.x, origin.y)
}