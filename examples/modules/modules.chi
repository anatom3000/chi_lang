import .math
import .utils

def do_nothing() {
    // ...
}

def main() {
    let a = math.add(3, math.add(2, 4));

    let origin = math.point.Point {
        x: 0.0,
        y: 0.0,
    }

    utils.printf("Math: 2+3+4=%i\n", a)
    utils.printf("The origin is at: (%f, %f).\n", origin.x, origin.y)
}