import modules.utils

import .point
import .notpoint

def add(a: int, b: int) -> int {
    utils.printf("Adding %i and %i\n", a, b)

    return a + b
}

def mul(a: int, b: int) -> int {
    return a * b
}