extern "<stdio.h>" {
    def printf(fmt_str: &char, ...)
}

def increment(i: &int) {
    // TODO: Operator-Assign
    *i = *i + 1
}

def main() {
    let a = 41

    increment(&a)

    printf("%i\n", a)
}